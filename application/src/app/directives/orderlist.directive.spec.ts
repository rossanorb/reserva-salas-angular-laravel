import { OrderlistDirective } from './orderlist.directive';

describe('OrderlistDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderlistDirective();
    expect(directive).toBeTruthy();
  });
});
